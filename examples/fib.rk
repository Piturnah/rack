// Returns the nth 0-indexed fibonacci number.

// int -- int
fn fib in
  let n in
    // Push n if n is 0 or 1.
    n 2 < if n ret end

    // Otherwise, recurse!
    n 2 - fib 
    n 1 - fib +
  end
end

// --
fn main in
  5 fib print
  9 fib print
end

