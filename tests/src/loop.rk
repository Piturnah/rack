// Print all the numbers 0 to 100
// Printing 420 after 69

fn main in
  0 while 1 + dup 101 < do
    dup print
    dup 69 = if
      420 print
    end
  end
  
  // Nesting logic inside the while condn also works
  0 while
    true true and if 0 print end
  1 + dup 101 < do
    1 print
  end
end
